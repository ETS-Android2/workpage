<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jajimenez.workpage"
    android:versionCode="4"
    android:versionName="0.3.0">

    <uses-sdk android:minSdkVersion="16"
        android:targetSdkVersion="21" />

    <supports-screen android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true" />

    <application android:label="@string/app_name"
        android:description="@string/app_description"
        android:icon="@drawable/ic_launcher"
        android:theme="@android:style/Theme.Holo.Light">

        <activity android:name="MainActivity"
            android:label="@string/app_name">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="ViewActivity"
            android:label="@string/view_1"
            android:parentActivityName="MainActivity" />

        <activity android:name="EditTaskContextsActivity"
            android:label="@string/edit_contexts"
            android:parentActivityName="MainActivity" />

        <activity android:name="EditTaskTagsActivity"
            android:label="@string/edit_tags"
            android:parentActivityName="MainActivity" />

        <activity android:name="TaskActivity"
            android:parentActivityName="MainActivity" />

        <activity android:name="EditTaskActivity"
            android:parentActivityName="MainActivity" />

        <activity android:name="AboutActivity"
            android:label="@string/about"
            android:parentActivityName="MainActivity" />
    </application>
</manifest>
